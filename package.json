{
  "name": "spotify-listen-ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:functions": "tsc --project functions/tsconfig.json && cp package.json package-lock.json functions/lib/ && cp functions/.env functions/lib/.env && cd functions/lib && npm install --omit=dev",
    "lint": "tsc --noEmit",
    "deploy": "npm run build:functions && firebase deploy --only functions",
    "serve:functions": "npm run build:functions && firebase emulators:start --only functions"
  },
  "engines": {
    "node": "^20.0.0"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "date-fns": "^3.6.0",
    "dotenv": "^17.2.3",
    "express": "^4.19.2",
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.1.1",
    "p-queue": "^8.0.1",
    "p-retry": "^6.2.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.4.5"
  }
}